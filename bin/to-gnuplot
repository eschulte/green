#!/bin/sh

echo "set style fill solid"
while read line;do
    echo "plot '-' notitle with histogram"
    echo "$line"|sed 's/|.*$//;'|tr ' ' '\n'
    # echo "$line"|sed 's/|.*$//; s/[[:digit:]]\+ [[:digit:]]\+ //;'|tr ' ' '\n'
    echo "e"
done
